# Slate

Slate is a local-only view of a UC task ledger and chest freezer inventory.

Choose the private Slate folder containing `slate.config.json`. The configuration
names exactly two absolute Markdown files. Slate reads and watches only those
files; it does not copy their contents into Workshop or synchronize them.
